{
  "name": "exuberant",
  "type": "module",
  "scripts": {
    "dune": "esy dune",
    "dune:build": "esy dune build @react",
    "dune:build-verbose": "esy dune build --verbose",
    "dune:watch": "esy dune build --watch",
    "dune:clean": "esy dune clean",
    "format": "esy dune build @fmt --auto-promote",
    "format-check": "esy dune build @fmt",
    "build": "vite build",
    "preview": "vite preview",
    "dev": "vite --config vite.config.js"
  },
  "scriptsComments": {
    "dune": "# Run dune, OCaml's build tool",
    "dune:build": "# Build the Melange apps",
    "dune:build-verbose": "# Build the Melange apps in verbose mode",
    "dune:watch": "opam exec -- dune build --watch @react @node",
    "dune:clean": "# Cleans all Melange artifacts",
    "format": "# Formats the Melange sources using ocamlformat",
    "format-check": "# Checks that the Melange sources have the right formatting (read-only)",
    "build": "# Bundle the JavaScriptapps generated by Melange",
    "preview": "# Serves the React app in a local server",
    "dev": "# Runs a development server"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "@rollup/plugin-node-resolve": "^15.3.0"
  }
}
